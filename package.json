{
  "name": "json-server",
  "version": "1.0.0",
  "description": "JSON server utilizado para o projeto LABMedical",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch data/db.json"
  },
  "author": "André Hopf",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
